 <hello world>
  {app title chillout}
 <app function= set up status,video call,voice note,message
   <app element> block contact
           unblock contact
    <app.db.get settings()then function (Settings)
    <form name = login
       Password:confirm password
  Unit:function
 This element (can view/app/views/sign
    this element.parent ()add (login-screen)
  
App.db.get settings ()then function(settings)
 App.aftr.(settings;settings)
 self.element.find (login-remember

        App.db.get settings (logged Account () then function
          if (account)
           self.options.aftr.(username,account
            self.options.aftr(password account
   (print) block a contact
           unblock contact
       self.options.(chat account
 Self.options.see online users
     
 <app function> add contact
      app.db.get settings set your profile picture
    App.function view status and last sechillout
{App title chillout}
